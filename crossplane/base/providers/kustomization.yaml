apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Provider CRs are cluster-scoped — no namespace needed here.
# These are shared across all environments; only versions may differ per overlay patch.
resources:
  - provider-family-azure.yaml
  - provider-azure-resources.yaml
  - provider-azure-storage.yaml
